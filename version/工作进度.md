# 工作进度

## 2026-07-27

### MPV 核心升级

- 将 MPV 核心从 v0.41.0-198 (2月20日构建) 升级到 v0.41.0-860 (7月6日构建)
- 升级来源：dyphire/mpv-winbuild 最新发布版
- 更新了约 662 个上游提交，包含最新的 libplacebo 和 FFmpeg
- 创建了更新器配置文件 settings.xml（x86_64 标准版）
- 初始化了项目进度记录系统

**下一步建议**：
- 根据个人喜好调整 mpv.conf 配置
- 测试播放确认一切正常
- 考虑是否升级 yt-dlp

### 汉化 stats.lua 统计信息 OSD

- 从 mpv 官方源码下载内置 stats.lua 放到 portable_config/scripts/ 覆盖内置版本
- 全面翻译了 OSD 统计信息界面的英文文本为简体中文
- 覆盖 6 个信息页：默认信息、扩展帧时间、缓存统计、键位绑定、轨道信息、内部性能
- 翻译量 100+ 字段，包含视频/音频/缓存/HDR/编解码器等各类标签
- 保留了 HDR10+、PQ(Y) 等不需要翻译的技术标准名称
- 脚本语法加载验证通过（mpv 无报错）

**下一步建议**：

- 用真实视频文件测试统计界面的完整显示效果
- 可继续调整翻译以更贴合个人习惯

## 2026-07-29 14:58

### 着色器与视频滤镜菜单分类审查

- 统计并核对了菜单中的 388 个着色器项目、387 个实际 GLSL 文件和 13 个 VapourSynth 脚本。
- 确认当前菜单按算法目录完整展开，混合了处理用途、算法家族、硬件后端和质量变体，因此很难判断该用什么。
- 发现低清视频会自动加载 `FSRCNNX+`，手动选择其他超分时可能重复处理。
- 当前显卡为 AMD Radeon RX 6600，菜单中的 NVIDIA 专用 VS 项目不适用。
- 已形成按使用目的重组的建议：常用预设、片源修复、放大、色度、锐化、流畅度、画面变换和专家库。
- 本阶段只完成研究与方案整理，尚未改动播放器菜单配置。

**下一步建议**：

- 将日常菜单精简为少量互斥预设，并为已启用方案增加状态提示。
- 把完整算法目录移入专家区，避免普通使用时误开、叠加或选择不兼容项目。

## 2026-07-29 15:23

### 完成画质处理菜单优化

- 新增“画质处理”一级菜单，按使用目的组织常用方案、单项处理、片源修复和流畅度。
- 将 `Ctrl+0～9` 改为互斥画质方案，不再意外叠加多个超分、降噪或锐化着色器。
- 为常用画质方案和流畅度模式增加动态勾选状态，可以直接看出当前启用了什么。
- 针对 RX 6600 保留 MVT-LQ、RIFE-DML 等合适入口，将 NVIDIA 专用项目移入专家区。
- VapourSynth 改为带标签切换，不再清空翻转、去色块等其他视频滤镜。
- 停用低清视频自动超分，改为手动选择，避免切换文件时覆盖当前画质方案。
- 完整的 387 个着色器仍保留在“专家工具 > 着色器库”，没有删除任何算法文件。
- 已验证所有常用方案、菜单勾选、VS 切换、配置语法和专家库完整性。

**使用建议**：

- 日常优先使用“动画柔和修复”或保持“关闭自定义着色器”。
- 720p 真人视频可选“通用高清放大”，更低清源可选“通用低清修复”。
- 需要肥皂剧式补帧时先试 MVT-LQ，RIFE-DML 负载更高。

## 2026-07-29 15:46

### 恢复着色器与视频滤镜一级分类

- 根据实际使用反馈，撤销“画质处理 / 专家工具”的深层菜单结构。
- 恢复“着色器”和“视频滤镜”两个一级菜单，完整算法库不再藏在专家区。
- 387 个 GLSL 文件继续按原技术家族直接分类，全部可从“着色器”进入。
- 推荐区缩减为六个按真人、动画和分辨率命名的场景入口，能一眼判断适用片源。
- CfL、kBFDN、SSim 和 SGEDS 快捷项并回对应技术分类，不额外制造新分类。
- 视频滤镜重新按片源修复、流畅度、画面变换、错误标记修复、帧率改写、色彩调整、管理和 VapourSynth 分类。
- 保留方案互斥、动态勾选、VapourSynth 安全切换和停用自动低清超分等底层改进。
- 已通过 mpv 实际菜单数据和配置语法验证；着色器库覆盖 387/387，零缺失、零重复。

## 2026-07-29 15:58

### 补帧、超分改为用途直达

- 移除视频滤镜菜单里需要先进入的“VapourSynth”中间层。
- “补帧”“超分”“降噪”提升为视频滤镜最前面的直属分类。
- 补帧直接提供 mpv 轻量插值、MVT-LQ、RIFE、DRBA、SVP 等方案。
- 超分直接提供 UAI-DML、UAI-MIGX、UAI-NV-TRT 和 ArtCNN。
- 方案名称直接标明 AMD、Intel、NVIDIA、负载和依赖要求。
- 保留安全替换与动态勾选，避免多个 VS 方案意外叠加。
- 已用实际 mpv 菜单验证 RIFE-DML 和 UAI-DML 的加载与状态显示。

## 2026-07-29 16:21

### 着色器改为用途分类并保留专家库

- 为全部 387 个着色器建立“日常按用途、专家按家族”的双索引。
- 日常菜单分为超分与缩放、修复与去模糊、锐化与细节、降噪与平滑、抗锯齿与抗振铃、去色带、色度修复、色彩与观感、去交错、画面工具与特效。
- 每个用途继续按片源类型和算法家族分层，避免大量项目堆在同一层。
- AMD、Anime4K 等混合家族会按实际功能出现在不同用途下。
- 新增“专家库”，完整保留原来的收藏、AMD、Anime4K、FSRCNNX、RAISR 等 35 个家族。
- 双索引只增加菜单引用，不复制着色器文件，不额外占用素材空间。
- 用途索引与专家索引均覆盖 387/387；实际 mpv 菜单读取约 7 ms，快捷方案勾选正常。

## 2026-07-29 16:52

### 允许三类滤镜叠加并改进状态显示

- 补帧、超分和降噪改用三个独立槽位，可以同时启用一种补帧、一种超分和一种降噪。
- 同类方案仍然互斥，切换补帧不会关闭当前超分或降噪。
- mpv 轻量插值只替换 VS 补帧，不再误关超分与降噪。
- 新增竖排画质状态 OSD，分别显示着色器数量、视频滤镜数量、每个启用项和 mpv 轻量插值状态。
- 状态快捷键确定为 `Ctrl+Alt+0`；菜单中直接写出快捷键。
- “清空全部着色器 · Ctrl+0”提升为着色器直属二级菜单。
- 已实测 RIFE-DML、UAI-DML 和 CCD 同时启用并分别勾选；同类切换和一键清空正常。

## 2026-07-29 17:46

### 完成菜单优化提交前复核

- 再次使用 mpv 实际加载新增状态脚本、输入配置和 profiles 配置，启动及退出正常。
- 用途索引和专家库均完整覆盖本地 387 个着色器文件，没有失效路径。
- `git diff --check` 通过。
- 已确认远程仓库没有新增提交，可以安全提交当前菜单优化。
- 下一步先提交并推送，再按实际变动决定只重建必要的 Release 包。

## 2026-07-29 17:56

### 提交菜单优化并完成 v1.1.0 打包

- 菜单优化已提交为 `175b4f4` 并推送到 GitHub。
- 根据 `v1.0.0` 之后的实际变化，将新版本确定为 v1.1.0。
- 本次只修改配置、菜单和状态脚本，因此只重建配置包与基础包。
- 着色器、VapourSynth、模型和工具没有变化，v1.0.0 extras 可以继续使用，无需重新上传约 2.6 GB。
- 打包脚本新增 `-SkipExtras` 开关，可跳过未变化的大型 extras 包。
- 两个新包均通过 7z 完整性检查、内容隔离检查和解包启动验证。

**待发布文件**：

- `mpv-config-v1.1.0.7z`（32.3 MB）
- `mpv-base-v1.1.0.7z`（74.6 MB）

## 2026-07-29 18:02

### MPV Portable v1.1.0 已发布

- 已创建并推送 `v1.1.0` 标签。
- 已发布 GitHub Release：
  https://github.com/maxzrb/mpv-portable/releases/tag/v1.1.0
- 配置包与基础包均已上传成功，GitHub 校验摘要与本地 SHA-256 完全一致。
- Release 为正式版本，不是草稿或预发布版本。
- extras 内容没有变化，发布说明已注明继续搭配 v1.0.0 extras 使用。

## 2026-07-29 18:48

### 精简 SVP 菜单并完成 v1.1.1 打包

- 补帧菜单中的“SVP Pro · 需安装 SVP”已精简为“SVP”。
- 已确认播放器使用便携包自带的 SVPFlow 插件，不需要另外安装 SVP 软件。
- 菜单配置和 SVP 实际补帧均通过 mpv 运行验证。
- 本次只修改菜单文案，因此仅构建配置包与基础包，extras 继续复用 v1.0.0。
- `mpv-config-v1.1.1.7z` 与 `mpv-base-v1.1.1.7z` 均通过完整性、文案和内容隔离检查。
- 下一步提交并发布 GitHub Release v1.1.1。

## 2026-07-29 18:51

### MPV Portable v1.1.1 已发布

- 菜单修正已提交为 `9735802`，分支和 `v1.1.1` 标签均已推送。
- GitHub Release：
  https://github.com/maxzrb/mpv-portable/releases/tag/v1.1.1
- 配置包与基础包已上传，远程文件大小和 SHA-256 均与本地一致。
- Release 为正式版本；extras 没有变化，继续使用 v1.0.0 分卷包。

## 2026-07-29 23:22

### 完成 Windows 原生 LSFG Vulkan Layer 研究接入

- 建立仅本地的 `research/lsfg-windows` 分支，未向公开仓库推送。
- 核验 Lossless Scaling 3.2.2 的 `Lossless.dll`，确认 lsfg-vk 所需 SPIR-V 模型资源完整。
- 导入并移植 lsfg-vk：支持 Win32 外部内存、外部时间线信号量、Vulkan Loader 和便携目录。
- 下载并校验 Windows 编译工具链，成功生成 `lsfg-vk-layer.dll`。
- 新增 `start-mpv-lsfg.ps1`，通过进程级环境变量注入 Layer，不写系统注册表。
- mpv 实际运行测试中已建立 320×240、2 倍补帧上下文并正常播放 30 帧，退出码为 0。
- 该接入位于最终 Vulkan 交换链，无需修改或重新构建 mpv，但字幕、OSD 和菜单也会进入补帧画面。
- 已生成 `release/mpv-lsfg-research-private.7z`（51,938,897 字节），SHA-256 为 `7C73A5EA24A9952ED44C77598634B7757435144D4C6B5444800F1C82C6E85B5E`，完整性检查通过。
- Lossless Scaling 文件和运行时 Layer 目录已加入 Git 忽略规则；当前没有提交、推送或更新公开 Release。

## 2026-07-30 00:02

### 将 LSFG 测试入口加入补帧菜单

- “视频滤镜 → 补帧”现在直接提供 LSFG 2×质量、2×性能、3×质量和 4×质量。
- 选择 LSFG 后会记住当前时间、暂停状态和播放列表，然后自动用 Vulkan Layer 重启续播。
- “关闭补帧”可以从 LSFG 模式重启回普通 mpv，不需要手动关闭窗口再打开文件。
- LSFG 启用期间，mpv 插值、RIFE、SVP 等其他补帧入口会变灰，防止两种补帧同时运行。
- 画质状态 OSD 已增加 LSFG 倍率和质量/性能模式。
- 已实际验证普通 mpv 切换到 LSFG、LSFG 切回普通 mpv，以及 2×补帧上下文建立。
- 私有研究包已更新为 51,957,616 字节，SHA-256：
  `69E91F8501A5E1891D8F0E96D6981B6FD694E1BAE20D4151CC0096A800AC97B1`。
- 当前仍只在本地研究分支，尚未提交、推送或更新公开 Release。

## 2026-07-30 00:25

### 增加 LSFG 实时帧率覆盖层与滤镜管理入口

- “视频滤镜”菜单前两项新增“查看当前启用项”和“清空全部滤镜”，布局与着色器菜单一致。
- 清空功能会同时清除完整视频滤镜链、关闭 mpv 插值；如果正在使用 LSFG，还会按当前进度重启回普通 mpv。
- Windows Layer 现在直接统计成功提交的原始帧和生成帧，每 0.5 秒输出真实 Present 速率。
- LSFG 启用后左上角默认显示倍率、质量/性能模式、原始 FPS 和实时 FPS。
- 覆盖层可从“视频滤镜 → 补帧 → LSFG 帧率覆盖层”随时开关。
- 画质状态 OSD 也会显示 LSFG 的“原始 FPS → 实时 FPS”。
- 30 fps 合成视频实测为 30.01 → 60.01 FPS；窗口后台运行时会如实反映 DWM 节流后的较低速率。
- “实时 FPS”表示提交到 Vulkan 交换链的帧率，不等同于显示器面板最终物理扫描率。
- 更新后的私有研究包为 52,035,917 字节，SHA-256：
  `3B4A08F24F47885960A259ED71779FBA98DAE1272231FA026ACDAD840E568F8C`。
- 当前仍未提交、推送或更新公开 Release。

## 2026-07-30 00:39

### LSFG 遥测跟随 Tab 常驻统计 OSD

- 确认 Tab 实际调用的是原大写 `I` 的常驻 stats OSD，而不是文件浏览器。
- LSFG 帧率面板现在默认隐藏；按 Tab 打开 stats 时同步显示，再按 Tab 或关闭 stats 时同步隐藏。
- 同步依据是 stats 脚本公布的真实开关状态，不会因为其他关闭方式导致两边状态错位。
- 遥测面板已移动到屏幕右上角，与左侧 stats OSD 同屏但不遮挡。
- 实际按键测试通过：初始两者均关闭，第一次 Tab 同时开启，第二次 Tab 同时关闭。
- 截图验证中，左侧显示源视频统计，右侧显示 LSFG 30.1 → 60.1 FPS。
- 私有包已加入修改后的 `stats.lua`，新包大小 52,051,306 字节，SHA-256：
  `8C9BA7CA18B1BA40FBEF89BF0B0AC44490EFD752C9220DAF8BE81CB8EF512C3E`。
- 当前仍未提交、推送或更新公开 Release。

## 2026-07-30 00:56

### 为四类安装包增加覆盖顺序编号

- 包名现在按安全覆盖顺序固定为：
  `01 Base → 02 Config → 03 Extras → 04 LSFG 私有包`。
- 同版本 Base 已经包含 Config，02 可以跳过；如果四类包全部安装，仍按编号从小到大解压。
- LSFG 私有包必须最后覆盖；重新安装或更新 Base/Config 后，需要再次应用 04 私有包。
- 根 README、Extras 包内说明和私有包内说明均已写明完整安装顺序及 Extras 分卷解压方法。
- 打包脚本语法检查通过；临时实际生成的 01、02、04 三个归档均通过完整性和包内 README 核验。
- Extras 约 2.6 GB，本次没有重新压缩，只核对了 03 文件名和包内说明生成逻辑。
- 当前改动尚未提交、推送，也没有更新公开 Release。

## 2026-07-30 01:05

### LSFG 研究功能验收并合并到主分支

- 已确认仓库实际主分支是 `master`，不是 `main`。
- LSFG Windows Vulkan Layer、补帧菜单、帧率遥测、stats 同步以及四包编号已完整提交并无冲突合并到本地 `master`。
- 研究提交为 `a9732f6`，合并提交为 `ce60088`；研究分支继续保留，方便后续追溯。
- 合并前重新完成 Windows Layer 增量构建、PowerShell 语法检查、mpv Lua 脚本加载和 Git 空白检查，结果均通过。
- `Lossless Scaling/`、`Lossless.dll`、私有包、运行时目录和临时测试包均未进入 Git；只合并了 GPL 研究源码、可重新构建的 Layer 产物及接入代码。
- 当前只完成本地合并，尚未推送 GitHub，也没有创建新版本、Tag 或更新公开 Release。

## 2026-07-30 01:15

### 第 04 包改为可公开发布的 LSFG 扩展包

- 旧私有包曾整包带入 Steam 的 Lossless Scaling 目录，共包含 433 个 Steam DLL；实际补帧只需要用户自行提供其中的 `Lossless.dll`。
- 新包改名为 `04-mpv-lsfg-addon.7z`，不包含任何 Steam DLL、EXE 或模型资源。
- 用户购买并安装 Steam 版 Lossless Scaling 后，只需把安装根目录的 `Lossless.dll` 复制到 `<mpv根目录>\Lossless Scaling\Lossless.dll`。
- `LosslessScaling.dll`、语言资源 DLL、.NET/WPF 运行库和 EXE 均不需要复制。
- 公开包只保留一个可公开分发的 GPL `lsfg-vk-layer.dll`，并附带对应源码和许可证信息。
- 新包大小为 2,002,853 字节，SHA-256：
  `641DB5E204F701BE6C4BBF117321DB59080A8E22C8D6DADEAB7A4821CD88A9E9`。
- 7-Zip 完整性和内容门禁检查通过：1 个 DLL、0 个 EXE、0 个 Steam 二进制。
- 旧私有包已从 `release` 移出，保存在 Git 忽略的 `tmp/private-archive-backup/` 中作为可恢复备份。
- 当前打包脚本和 README 调整尚未提交、推送或上传 GitHub Release。

## 2026-07-30 02:02

### 统一 v1.2.0 包名、清理打包废料并生成个人全量包

- 所有公开包统一使用 v1.2.0，并固定为 `01 Base → 02 Config → 03 Extras → 04 LSFG` 的覆盖顺序。
- 新增统一入口 `build-all-packages.ps1`；加 `-IncludePrivate` 可同时生成个人私用全量包。
- 新增 `build-full-private.ps1`，按编号解压四个公开包，再加入个人正版 Steam 安装中的 `Lossless.dll`。
- 打包阶段会自动排除 Python 缓存、日志、临时文件和调试产物；本次从 Extras 清除 121 个 `__pycache__` 目录和 1321 个生成文件，约 21.9 MiB。
- 内容审计未发现 `release`、`build`、`tmp`、`.git`、旧 Release 或其他开发目录被套入归档。
- Extras 和 LSFG 与 Base/Config 均无路径重叠；Config 与 Base 的重叠是更新子集设计。
- 已生成 `01-mpv-base-v1.2.0.7z`、`02-mpv-config-v1.2.0.7z`、`03-mpv-extras-v1.2.0.7z.001/.002`、`04-mpv-lsfg-addon-v1.2.0.7z` 和 `mpv-full-private-v1.2.0.7z`。
- 所有归档均通过 7-Zip 完整性测试；SHA-256 已写入 `version/版本迭代记录.md`。
- 旧输出已移动到 Git 忽略的 `tmp/release-backup-before-v1.2.0/`，可随时恢复。
- 当前改动尚未提交、推送或上传 GitHub Release；个人私用全量包不得公开上传。

## 2026-07-30 02:21

### MPV Portable v1.2.0 已发布

- LSFG Windows Vulkan Layer、补帧菜单、帧率遥测、公开扩展包和新版打包体系已提交并推送到 `master`。
- 发布提交为 `fce95b3`，`v1.2.0` Tag 指向同一提交。
- GitHub Release：
  https://github.com/maxzrb/mpv-portable/releases/tag/v1.2.0
- 已上传 01 Base、02 Config、03 Extras 两个分卷和 04 LSFG 共五个公开资产。
- GitHub 远端显示五个资产全部上传完成，文件大小与 SHA-256 和本地一致。
- 远端 README 已更新安装顺序、LSFG 自备 `Lossless.dll` 方法，以及公开包和个人包边界。
- 个人私用全量包没有上传，继续只保留在本地。

## 2026-07-30 11:18

### 整理 LSFG 补帧菜单

- 新增 `视频滤镜 → 补帧 → LSFG` 子菜单。
- LSFG 2×质量、2×性能、3×质量和4×质量四个预设已统一收进该子菜单。
- “查看 LSFG 状态”也归入 LSFG 子菜单；关闭补帧和其他补帧算法仍保持原位置。
- 菜单嵌套规则和 Git 空白检查均已通过，改动尚未提交或推送。

## 2026-07-30 11:45

### 左上角置顶图标改为真正按钮

- 原来的 `📌` 只是窗口标题中的状态文字，点击后会落入“鼠标左键暂停”的全局绑定。
- 已从标题中移除静态图标，并在 uosc 顶栏左上角增加独立的置顶按钮。
- 单击按钮会切换窗口置顶并显示状态；置顶生效时按钮保持高亮。
- 使用 `Alt+T` 切换置顶时，按钮状态也会同步刷新。
- Lua 语法、完整配置加载和 Git 空白检查均已通过，改动尚未提交或推送。

## 2026-07-30 12:51

### 安装 yt-dlp 并纳入公开基础包

- 从官方 GitHub 最新稳定 Release 安装 `yt-dlp.exe` 2026.07.04，与 `mpv.exe` 放在同一目录。
- 通过官方 `SHA2-256SUMS` 校验，SHA-256 为：
  `52FE3C26DCF71FBDC85B528589020BB0B8E383155CFA81B64DD447BBE35E24B8`。
- 程序可正常枚举 1752 个网站提取器，包含 YouTube 和 Bilibili。
- 从 mpv 根目录之外启动播放器，内置 ytdl hook 仍能自动发现并调用该程序。
- 使用真实 HTML5 视频网页完成端到端测试：网页解析、媒体地址回传和 mpv 首帧解码均成功。
- `build-release.ps1` 已将 `yt-dlp.exe` 加入 `01-mpv-base`，后续公开基础包不会漏装。
- 没有显式指定显卡或写入系统 PATH；可执行文件本身继续由 Git 忽略。
- 当前打包脚本及前序菜单、置顶按钮改动尚未提交或推送。

## 2026-07-30 13:37

### 检查播放器、滤镜、脚本和工具更新

- mpv 核心、yt-dlp、uosc、uosc_danmaku 和 LSFG Vulkan Layer 已是当前上游版本。
- 有正式新版的基础组件：
  - VapourSynth R73 → R78
  - Python 3.14.3 → 3.14.6
  - 7-Zip 25.01 → 26.02
  - TorrServer MatriX.141 → MatriX.142.2
  - umpv-go 1.4.0 → 1.5.1
- GLSL 滤镜库本地有 387 个，上游有 429 个：
  - 366 个同名文件完全相同。
  - 上游新增或替换了 63 个，本地另有 21 个旧版/专用文件。
  - 主要变化集中在 ACNet、QCOM、FSRCNNX、ESPCN、ESRGAN 和 RAISR。
- 多个 Lua 脚本存在上游变化，包括 thumbfast、sub-fastwhisper、sub-assrt、file-browser 和 5 个 dyphire 脚本；这些文件含本地定制，需要手工合并，不能直接覆盖。
- Faster-Whisper-XXL 目录目前为空，实时语音字幕功能实际缺少可执行程序；上游最新 Pro 为 r3.256.1。
- “一键更新脚本和着色器”当前不可靠：
  - PlayKit 已改用 `main`，配置仍按默认 `master` 拉取。
  - quality-menu 白名单文件名拼写错误。
  - 更新脚本不会根据 subprocess 失败正确报告结果。
- 本轮只完成检查，没有替换任何播放器、滤镜、脚本或工具。
- 建议先提交现有菜单和置顶按钮改动，再按“小工具 → 脚本合并 → GLSL 与菜单 → VapourSynth/Python 整套”的顺序升级。

## 2026-07-30 14:58

### 一键更新改为安全基线与三方合并

- 已重写“一键更新脚本和着色器”，菜单消息现在能正确触发异步更新。
- 更新器不再直接覆盖文件：
  - 首次发现差异时保留本地版本。
  - 仅上游变化时才安全更新。
  - 本地和上游同时变化时按旧上游基线三方合并。
  - 覆盖前自动备份；冲突候选、基线、状态和报告写入 `portable_config/cache/manager/`。
  - 上游移除文件时只报告，不删除本地文件。
- 已修复 PlayKit 主分支、quality-menu 拼写、stax 文件名、Eisa 路径和 file-browser addons 映射；未安装的 trakt-scrobble 已禁用。
- GitHub 更新源改用无工作区 Git 对象树，避免 Windows 非法文件名、换行转换和不必要的完整检出。
- 为公开版控制体积，缺失脚本与 63 个新增着色器默认不自动安装；现有 21 个本地独有着色器继续保留。
- 最终真实稳定态检查结果：
  - 445 个文件与上游一致。
  - 15 个差异文件全部保留本地版本。
  - 74 个缺失/可选文件按策略跳过。
  - 0 自动覆盖、0 三方合并、0 错误。
- 更新前快照保存在 `tmp/pre-manager-update-20260730-135035/`；前序 input、mpv 和 uosc 个性化文件经 SHA-256 复核未发生变化。
- PowerShell 5.1、JSON、LuaJIT、mpv 隔离加载、菜单消息和 Git 空白检查均通过。
- VapourSynth/Python、7-Zip、TorrServer、umpv-go 和 Faster-Whisper 本轮未升级，后续仍应分批处理。
- 当前改动尚未提交或推送。

## 2026-07-30 16:20

### 安全合并差异文件并升级独立二进制

- 建立 `tmp/pre-safe-merge-binary-upgrade-20260730-150859/` 回滚快照后，安全合并了更新器报告的 15 个差异文件。
- 12 个脚本/文档从其历史上游版本快进到当前版本；两个小脚本只处理文件尾差异；`aWarpSharp3_RT.glsl` 确认原本已是当前 PlayKit 版本。
- 修复 `chapterskip.lua` 同名但功能不同的更新源覆盖风险，并用 `git hash-object --no-filters` 消除着色器假冲突。
- 新版 trackselect 已自行识别 URL 和 magnet 协议，删除了不再受支持的 `special_protocols` 配置项。
- 已完成以下运行组件升级：
  - 7-Zip 25.01 → 26.02
  - TorrServer MatriX.141 → MatriX.142.2
  - umpv-go 1.4.0 → 1.5.1
  - Python 3.14.3 → 3.14.6
  - Faster-Whisper-XXL 公开版 r245.4（原目录为空）
- Faster-Whisper 官方包大小为 1,424,256,246 字节，本地 SHA-256 为
  `237DEE23939CDABFC96EF859FC5E584B842C3A5557E0D2CA744E1F87C14C5844`；
  5127 个文件通过压缩包完整性测试。
- Extras 打包脚本改为：检测到 `faster-whisper-xxl.exe` 时复制完整公开版，未安装时才建立空目录；没有设置显卡或设备参数。
- VapourSynth R78 已在独立目录完成 Python 和 mpv 烟测，但其新 wheel 布局要求进程设置 `VSSCRIPT_PATH`；根目录 DLL 方式无法保持直接双击 `mpv.exe` 的便携体验，因此正式环境暂留 R73。
- 15 个 Lua 文件语法、完整 mpv 配置三帧播放、Python/VS 取帧、各二进制帮助入口和安全更新器全量检查均通过。
- `input.conf`、`mpv.conf` 和两个 uosc 置顶按钮文件与更新前个性化快照哈希一致，没有被组件升级覆盖。
- 本轮与之前未提交的菜单、置顶按钮、yt-dlp 和安全更新器改动仍未提交或推送。

### 清理 R78、Faster-Whisper 拆分独立增量包、LSFG 重编号

- 确认 VapourSynth R73 为明确支持 Windows 7 的最后版本，暂不升级 R78。已清理 `tmp/` 下共约 340 MB R78 试验文件。
- 核对本轮升级的所有二进制文件名（Python 3.14.6、7-Zip 26.02、TorrServer 142.2、umpv-go 1.5.1）与打包脚本引用完全匹配，无需同步改动。
- Faster-Whisper 从 03 Extras 拆分为独立的 04 增量包，方便用户选择是否下载约 4.4 GB AI 字幕运行时。
  - 新增 `build-fasterwhisper-public.ps1`，产物为 `04-mpv-fasterwhisper-addon-vX.Y.Z.7z`。
  - 包内只包含 Faster-Whisper-XXL 公开版，门禁检查防止混入无关文件。
- LSFG 公共扩展包从 04 重编号为 05，与 FW 增量包交换序号。
  - `build-lsfg-public.ps1`、包内 README 和根文档全部更新。
- 打包体系统一更新：
  - 01 Base → 02 Config → 03 Extras → 04 FW → 05 LSFG
  - `build-all-packages.ps1` 改为五个包连续构建。
  - `build-full-private.ps1` 合并链加入 04 FW 并重新验证必需文件列表。
  - 根 `README.MD` 所有"四类包"改为"五类包"。
- 五个 PowerShell 打包脚本通过系统语法检查，`git diff --check` 通过。
- 本轮打包脚本和文档改动与之前大量未提交的改动仍未提交或推送。

### 发布 v1.3.1：五包重构 + LSFG 帧率修复 + 依赖升级 (2026-07-31)

- 包结构重组为 01 Base → 02 Extras → 03 FW → 04 LSFG → 05 Config。
- Base 包保证基本播放可用，Config 包作为最终个人设置覆盖层。
- FW 从 Extras 拆分为独立增量包，LSFG 包含控制脚本以保证联动可用性。
- 个人全量包以 01→02→03→04→05 顺序合并，并完整备份 Lossless Scaling 目录。
- LSFG 帧率修复：
  - 利用 `estimated-vf-fps` 通过环境变量矫正遥测显示。
  - 尝试了 FIFO 交换模式、display-fps-override、VkImage 比较、生成限流等方案。
  - VkImage 方案因 mpv 重复渲染同一帧导致句柄不同而失败，限流方案导致播放器死锁。
  - 最终仅保留遥测修正，不做帧生成干预。
  - 已知限制：Optimus 双显卡笔记本 LSFG 需约 30 秒预热，播放仍可能有轻微卡顿。
- 依赖升级：Python 3.14.6、7-Zip 26.02、TorrServer 142.2、umpv-go 1.5.1、Faster-Whisper r245.4、yt-dlp 2026.07.04。
- VapourSynth R78 试验文件已清理，保留支持 Win7 的 R73。
- v1.3.1 已推送到 GitHub Release，五个公开包可供下载。

### 2026-08-04 14:21：新增 Anime4K 官方 HQ/Fast 标准预设

- 放弃引入 ModernZ，继续保留当前定制 uosc 界面。
- 新增 Anime4K v4 官方标准组合，共 12 套：
  - HQ：Mode A、B、C、A+A、B+B、C+A，面向 GTX 1080、RTX 2070、RTX 3060、RX 590、Vega 56、5700 XT、6600 XT 及以上。
  - Fast：Mode A、B、C、A+A、B+B、C+A，面向 GTX 980、GTX 1060、RX 570 及以下。
- 菜单位置：`着色器 > 推荐 > Anime4K 标准预设`，按 HQ 和 Fast 分组。
- 模式提示已写进菜单：A 主要适合多数 1080p，B 主要适合多数 720p，C 适合 480p 或低退化图像；A+A、B+B、C+A 仅建议至少 2× 放大时使用。
- 每个预设都会替换完整着色器列表，不会和之前选择的着色器意外叠加。
- 12 套方案全部完成 gpu-next/D3D11 实际编译和单帧播放测试，12/12 通过；菜单解析、UTF-8/LF 和 `git diff --check` 也全部通过。
- 本轮改动尚未提交或推送；工作区原有 `.gitignore`、打包脚本和 `mpv.conf` 改动均保持不变。

### 2026-08-04 14:24：精简 Anime4K 预设菜单

- 按你的要求，菜单里不再写具体显卡型号（GTX、RTX、RX、Vega 等），只保留 `HQ` 和 `Fast` 两档以及每个模式适合的画面类型。
- 12 套预设本身没有变化，配置组和着色器链完全保持原样。
- 菜单解析和格式检查均通过，尚未提交。

### 2026-08-04 15:05：v1.3.2 构建完成，准备发布

- 已提交 Anime4K 预设、字幕颜色和全量包修复（`d3f41da`）。
- 清理了旧的 build/release 缓存和产物，释放约 33 GB。
- 完成 01 Base、02 Extras 分卷、03 FW、04 LSFG、05 Config 五个公开增量包，以及个人全量包。
- 六个包全部通过 7-Zip 完整性测试，SHA-256 已写入版本迭代记录。
- 接下来创建 v1.3.2 Release，上传五个公开包；个人全量包仅保留本地。

### 2026-08-04 15:12：v1.3.2 已发布

- Release：https://github.com/maxzrb/mpv-portable/releases/tag/v1.3.2
- 01 Base、02 Extras 分卷、03 FW、04 LSFG、05 Config 五个公开包已上传。
- 个人全量包 `mpv-full-private-v1.3.2.7z` 仅保留本地，未上传 GitHub。
- 构建暂存目录已清理，Git 工作树干净。
